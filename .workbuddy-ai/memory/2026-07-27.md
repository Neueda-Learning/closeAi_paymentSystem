# 2026-07-27 — 项目启动：closeAi_paymentSystem

## 关键操作
- 用户：蒋罗，路径 `E:\Project\traningProject\closeAi_paymentSystem`
- 后端：Spring Boot 3.2 + Java 17 + MyBatis-Plus（已编译 30 文件 OK）
- 前端：Vue 3 + Vite + Element Plus（83 npm 包安装 OK）
- 数据库：dev profile 走 **H2 内存数据库**（无需 MySQL/Docker）

## 三个关键坑（已踩，下次直接规避）
1. **H2 scope=test 启动报错** `Cannot load driver class: org.h2.Driver`
   - 修法：`backend/pom.xml` 里把 h2 的 `<scope>test</scope>` 改成 `<scope>runtime</scope>`
   - 已改，保留
2. **Maven 路径转换** bash 里调 `mvn` 报 `ClassNotFoundException`，必须用 `mvn.cmd`
   - 直接调 `E:/Download/apache-maven-3.5.3/bin/mvn.cmd`
3. **环境变量 `SERVER__PORT=54172` 覆盖配置** Spring Boot 启动时端口不是 8080 而是 54172（且被 WorkBuddyAI 自己占用）
   - 用户 shell 里这个变量是 IDE/Win 配置注入的
   - 修法：启动时显式传 `--server.port=8080`（命令里加 `-Dspring-boot.run.arguments="--server.port=8080"`）

## 启动命令模板
```bash
# 后端（dev profile + H2 + 强制 8080）
cd "E:/Project/traningProject/closeAi_paymentSystem/backend"
"E:/Download/apache-maven-3.5.3/bin/mvn.cmd" -B spring-boot:run \
  "-Dspring-boot.run.profiles=dev" \
  "-Dspring-boot.run.arguments=--server.port=8080"

# 前端
cd "E:/Project/traningProject/closeAi_paymentSystem/frontend"
npm run dev
```

## 端口 / 入口
- 后端 API: `http://localhost:8080/api/payments`
- Swagger UI: `http://localhost:8080/swagger-ui.html`
- OpenAPI JSON: `http://localhost:8080/v3/api-docs`
- 前端: `http://localhost:5173/`（Vite 自动代理 `/api` → 8080）

## 种子账户（dev/H2 schema-h2.sql 自动加载）
ACC-00001 ~ ACC-00010，含 USD/EUR/GBP/CNY，余额从 0 到 10,000,000。

## 验证结果
- 完整链路：CREATED → VALIDATED → SENT → COMPLETED，4 条状态历史全记录 ✓
- 前端代理 /api/payments 返回 200 ✓
- 后端 PID 22040 监听 8080；Vite 监听 5173 ✓

## 数据库当前状态
- **MySQL 9.0.1 跑在 3306**（D:\App\mysql\mysql-9.0.1-winx64\，PID 22480）
- root 密码：`Payment@2026`（用 `--init-file` 重置过的，**不能再用 `--skip-grant-tables` 启动**，会崩 debug assertion）
- 数据库：`payment_system`（含 accounts/payments/status_history/idempotency_keys 4 表 + 10 种子账户）
- 应用用户：`payment_app` / `Payment@2026`（grant ALL on payment_system.*）

## 启动命令模板（MySQL 版，当前在用）
```bash
# 1. MySQL（后台）
"D:/App/mysql/mysql-9.0.1-winx64/bin/mysqld.exe" \
  --defaults-file="D:/App/mysql/mysql-9.0.1-winx64/my.ini" --console

# 2. 后端（不用 dev profile，直接连 MySQL）
cd "E:/Project/traningProject/closeAi_paymentSystem/backend"
DB_USERNAME="payment_app" DB_PASSWORD="Payment@2026" \
DB_URL="jdbc:mysql://127.0.0.1:3306/payment_system?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf-8" \
"E:/Download/apache-maven-3.5.3/bin/mvn.cmd" -B spring-boot:run \
  "-Dspring-boot.run.arguments=--server.port=8080"

# 3. 前端
cd "E:/Project/traningProject/closeAi_paymentSystem/frontend"
npm run dev
```

## Navicat 连接信息
- 主机：localhost / 127.0.0.1
- 端口：3306
- 用户：root
- 密码：Payment@2026
- 数据库：payment_system
- SSL：首次连取消勾选（默认开启会卡）

## 收尾决策（14:10）
- 用户决定今天到此为止，不再装 MySQL/Docker，继续在 H2 + dev profile 上跑
- 已完整读过 3 份核心文档：README.md、payment_processing.md、Dev_Guide_Payment_Processing_AI_Anomaly_Detection.md
- 已交付"现状总结 + 待办清单"（按 P0/P1/P2 排优先级）

## 计划变更（15:21）
- 用户改主意，要切到 MySQL + Navicat（已切换完成，见上文）

## 待办（下次接手从这里开始）
P0 核心：
- [ ] 新建 `risk_assessments` 表（schema-h2.sql + schema.sql）
- [ ] `RiskAssessment` entity + Mapper + Service
- [ ] AI 异常检测 Layer 1 规则引擎（5 条规则，Dev_Guide L213-265）
- [ ] Layer 2 统计异常检测（z-score + IQR，Dev_Guide L292-323）
- [ ] 把 hook 接进 `PaymentServiceImpl.processValidate`（已有占位符 L205）
- [ ] RISK_BLOCKED 错误码（HTTP 403，README 错误码表已有但代码未用）

P1 完整：
- [ ] 前端 `RiskScoreGauge.vue` 风险仪表盘
- [ ] 详情页 + 列表页加风险展示
- [ ] `GET /api/payments/{id}/risk-assessment` 端点
- [ ] AI 检测相关测试用例（Dev_Guide L590-634 给了 13 个场景）

P2 加分：批量/调度/通知/报表/冲正 — 看后续规划。